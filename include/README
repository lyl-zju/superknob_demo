Project public headers live in this directory.

For local setup, copy `secrets.example.h` to `secrets.h` and fill in the
Wi-Fi and fan credentials. `secrets.h` is intentionally ignored by Git.
See the repository root `README.md` for complete setup instructions.
